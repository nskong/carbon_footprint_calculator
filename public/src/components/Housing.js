import { Button } from 'antd';

export default function Food() {
  return (
    <div className="Food">
        <div id="Main-text">Choose a Housing Category</div>

        <Button size="large" type="primary">Electricity</Button>
    </div>
  );
}